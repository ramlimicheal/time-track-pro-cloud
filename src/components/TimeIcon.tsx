
import { Clock } from "lucide-react";

export const TimeIcon: React.FC = () => {
  return (
    <div className="flex items-center justify-center text-timetrack-blue">
      <Clock size={24} />
    </div>
  );
};
